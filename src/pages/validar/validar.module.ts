import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ValidarPage } from './validar';

@NgModule({
  declarations: [
    ValidarPage,
  ],
  imports: [
    IonicPageModule.forChild(ValidarPage),
  ],
})
export class ValidarPageModule {}
